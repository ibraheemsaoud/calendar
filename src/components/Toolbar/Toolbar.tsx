import * as React from "react";
import "./Toolbar.scss";
import { Button } from "controls/Button";

export const Toolbar = (): JSX.Element => {

  return (
    <div>
      <Button />
    </div>
  );
};
